<?
$hostname = "localhost";
$username = "root";
$password = "";
$dbname = "ecom";
$conn = mysql_connect($hostname,$username,$password);
if(!$conn)
die ("can't connect");
mysql_select_db($dbname,$conn)
or die("can't select db");
?>